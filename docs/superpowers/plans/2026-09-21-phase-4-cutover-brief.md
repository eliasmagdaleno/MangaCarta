# Phase 4 cutover — WeebCentral ships as a bundled package; the compiled Source goes

Repository: /Users/eliasmagdaleno/Manga-Reader (GitHub eliasmagdaleno/MangaCarta). You are in the S6 worktree `phase4-s6-weebcentral-installed`; its branch is merged. FIRST: `git fetch origin && git checkout -b eliasmagdaleno/phase4-cutover-bundled-weebcentral origin/main`. Commit early and often; push the branch and open a draft PR as soon as the first test is green so nothing is lost if you stop.

## Read first (owners of the decision — do not restate them in code comments, link them)
- `docs/adr/0003-extension-substrate.md` **Amendment 5** — the decision, five parts. Every clause below traces to it.
- `docs/superpowers/specs/2026-09-11-repository-format-design.md` **§12 Bundled repositories** — the shape.
- `docs/adr/0022-no-adult-source-in-the-release-build.md` Amendment 3 — why this is not a default repository.
- PR #192's "Cutover boundary" section — exactly what is deleted.
- `CLAUDE.md` — build commands (iPhone 17 Pro sim, id ADDAB2F8-38C7-4D44-97EA-4E98281CF691), pbxproj rules (`Views/` and `MangaCartaTests/` are NOT synchronized groups; use `xcp` or Xcode), CI's Xcode 16.4 / Swift 6.0 limits (no isolated conformances, no `nonisolated(nonsending)`, no `@concurrent`).

## Target
`MangaCarta/` app target + `MangaCartaTests/`; the seeded simulator fixture on the iPhone 17 Pro (see `scripts/seed-simulator.sh`); `docs/superpowers/plans/2026-09-11-phase-4-repository-format-installer.md` (add a "Cutover" slice row + brief pointer); `CLAUDE.md` "Current state" paragraph (one sentence: builtInSources() is MangaDex only; WeebCentral is a bundled package).

## Change — clauses, each needs a test whose mutation you report
1. **Bundled package resources.** Add `MangaCarta/Resources/BundledRepositories/weebcentral/index.json` + `engine.js` (+ declaration inline in the index, as format 1 does). The script bytes are `HTMLSelectorThemeEngine.bundleScript` verbatim (the test fixture `MangaCartaTests/__Fixtures__/weebcentral/repository-engine.js` is that plus a marker comment — make the bundled file the one copy; the fixture and the Swift constant go, or the fixture becomes a symlink/copy loaded from the bundle. ONE copy of the script in the repo when you are done; a structural test asserts that no Swift file contains the engine source). `scriptSHA256` in the index must match the bytes. Declaration = the WeebCentral declaration `PortFixtures.weebCentralJSON` already carries, `adult: none`.
2. **`BundledRepositoryTransport: RepositoryTransport`** — reads the bundle; `fetchIndex` validates via `RepositoryIndexValidator`, never returns `.movedPermanently`; `fetchScript` reads the resource; §10 size bounds apply. Test: index parses; script digest matches; a `mixed`/`adultOnly` declaration in a bundled index is refused at validation (§12 "Adult Sources") — add that rule where the validator is invoked for bundled indexes, not in the shared validator's general path unless it takes a flag.
3. **Fixed identity.** One constant `BundledRepositories.weebCentralRepositoryID: UUID` (a literal v4 UUID, generated once, declared in ONE place). Qualified id `<uuid>:weebcentral` via `ExtensionInstaller.qualifiedID`. Test: the id is stable across two `AppComposition` instances and equals the constant.
4. **First-launch install** — in `AppComposition` (or the registrar): if `RepositoryStore` has no record for the fixed UUID, add the bundled repository and install `weebcentral` through the ordinary `ExtensionInstaller` path (no acknowledgement can be needed: it is `none`-class). If a record exists and the Source is `uninstalled`, do NOT reinstall (A5 part 4). Every launch re-resolves the repository record's URL to the current bundle resource URL (§12 "Records") before restore. Tests: fresh store → installed + registered in `registry.sources` after MangaDex; uninstalled record → stays uninstalled, still listed as offered; stale bundle URL in the record → rewritten.
5. **App-update path.** Bump the bundled index `version` in a test double → `installer.refresh` on the bundled repo surfaces an available update, applied only through `updateBundle` (reader-confirmed), never automatically. Test both halves.
6. **Identity migration** — `WeebCentralIdentityMigration` (name it what fits the codebase) runs once before the installed Source registers: rewrites source id `"weebcentral"` → the qualified id in every persisted store that carries a source id. Enumerate them by grepping for `sourceId`/`sourceID` in every persisting type (`grep -rln 'sourceID\|sourceId' MangaCarta | grep -iE 'store|cache|preference'` is the starting list — it names at least `WorkStore`, `LibraryStore`, `HistoryStore`, `EntityResolutionStore`, `TasteProfileStore`, `SourcePreferenceStore`, `UpdateStateStore`, `ListingCountCache`); account for every hit in the PR, including the ones you decide need no rewrite and why. Idempotent (a marker or "nothing left to rewrite" — prefer the latter, it is self-healing). Tests: a fixture of each store with the bare id → migrated; running twice is a no-op; a store with no bare ids is untouched byte-for-byte; the Work identity (ADR-0001) survives — a Work with a `"weebcentral"` Listing resolves to the same Work id after migration.
7. **Delete the compiled Source.** Remove `MangaCarta/Models/WeebCentralSource.swift`, its entry in `SourceRegistry.builtInSources()` (MangaDex only, ADR-0003 A1), the compiled side of `WeebCentralPortTests`/`ExtensionPortHarness` (`PortFixtures.compiledWeebCentral()`), and every test that only exists to test the compiled type. `WeebCentralPortTests` becomes: bundled-installed Source vs the pinned port fixtures (the S6 `testInstalledPackageRunsWeebCentralAgainstPinnedPortFixtures` comparison, now against fixture *expectations*, since the compiled side is gone — pin the expected values as data). Grep for `WeebCentralSource` and `"weebcentral"` across `MangaCarta/`, `MangaCartaTests/`, `MangaCartaUITests/`, `scripts/` afterwards and account for every hit.
8. **Repositories screen.** A bundled repository shows no Remove and no Change URL (§12). Its Source shows Install/Uninstall/Disable/Enable like any other. Unit-test via `RepositorySettingsViewModel` or a small view-model predicate; a UI test is optional (CI's hermetic list is in `.github/workflows/ci.yml`; add to `-only-testing` if you add one).
9. **Fixture re-seed.** `scripts/seed-simulator.sh` seeds through the app's real stores (read it); `scripts/adr0019_seed.py` writes `works.json` with bare `weebcentral` ids by hand. Update whichever stamps the id so the seeded fixture carries the qualified id, OR rely on clause 6's migration running at the seeded app's next launch — pick one, say which in the PR, and prove it: after your change, launch the seeded app once on the Pro and show (a `plutil`/`jq` read of `works.json` in the app container) that no `"weebcentral"` bare id remains. Do NOT erase the simulator; copy the app container aside first (works.json was destroyed once this way).

## Constraints
- No second code path for "the one we ship": the bundled Source is an `ExtensionSource` restored by `ExtensionSourceRegistrar` like any install. Only the transport, the identity constant, the first-launch install and the screen's two hidden buttons may know a repository is bundled.
- No network for the bundled repo; no URL a reader can see or change.
- `Manga.sourceId` must be stamped with the qualified id on every path (the MangaDex `toManga` rule in CLAUDE.md).
- Keep `MangaSource`'s protocol unchanged.
- Swift 6.0-compatible syntax only (CI).
- Do not touch ADRs, the glossary or the format design; if you find them wrong, say so in the PR and stop that clause — do not "fix" the owner document.

## Ownership
You own the branch above and everything under `MangaCarta/`, `MangaCartaTests/`, `MangaCartaUITests/`, `scripts/`, the plan doc row, and the one CLAUDE.md sentence. Coordinate (ask) before touching `project.pbxproj` by hand — use `xcp` per CLAUDE.md, and check `git diff --stat` on the pbxproj immediately before each `git add`.

## Observable acceptance
- Full unit suite on the Pro: `xcodebuild -scheme MangaCarta -destination 'platform=iOS Simulator,id=ADDAB2F8-38C7-4D44-97EA-4E98281CF691' test -only-testing:MangaCartaTests -resultBundlePath /tmp/cutover-unit.xcresult`; report totals from the result bundle (passed/failed/skipped), not from the log tail. Then the hermetic UI classes named in `ci.yml`.
- `swiftlint lint --quiet` on every file you touched — zero errors (line length ≤ 200). CI runs SwiftLint and it failed #192 on this.
- PR body: a clause→test→mutation table for clauses 1–9 (mutate each clause separately, name the failing assertion), the list of stores clause 6 rewrites, the grep-account for clause 7, what clause 9 ran, and result-bundle totals. Title: `feat(extensions): cut over WeebCentral to a bundled package (ADR-0003 A5)`. Body ends with `Closes #168`? NO — leave #168 open; reference it as "Phase 4 cutover, #168".
- `worker_done --outcome succeeded` only when the PR is up with all of the above; otherwise `--outcome failed` with the table as far as it got and the branch pushed.
